import { TimeState } from '../../types';

const HMS_ZERO_TIME: TimeState = {
  hours: '00',
  minutes: '00',
  seconds: '00',
};

const MSMS_ZERO_TIME: TimeState = {
  minutes: '00',
  seconds: '00',
  milliseconds: '00',
};

export { HMS_ZERO_TIME, MSMS_ZERO_TIME };
